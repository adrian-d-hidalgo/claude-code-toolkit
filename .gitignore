# macOS
.DS_Store
._*

# Claude Code local files
.claude/settings.local.json

# Activation eval run artifacts (timestamped reports + live logs).
# The corpora (plugins/*/skills/*/tests/activation-evals.json) and the runner
# stay in git; results are transient and reproduced by re-running the harness.
.eval-runs/

# The baseline (.eval-baseline.json) at repo root IS committed when promoted.

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv*/
venv*/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Secrets — never commit any of these
.env
.env.*
!.env.example

# Logs
*.log

# IDE / editor noise
.vscode/
.idea/
*.swp
*.swo
*~
