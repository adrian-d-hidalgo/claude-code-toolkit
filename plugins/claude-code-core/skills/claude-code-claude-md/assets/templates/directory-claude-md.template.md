<!-- last-reviewed: YYYY-MM-DD -->

<!--
Template: <subdir>/CLAUDE.md (directory / monorepo-package level)

Identity: concatenated AFTER global + project CLAUDE.md. Loaded only when Claude works
inside this directory tree.

Include ONLY what differs from the project root. Do not repeat any project-level rule.

Target: ≤ 50 lines. If you find yourself writing more, ask whether the content belongs
at the project level instead.
-->

## Scope

<!-- One sentence: what's in this directory and how it differs from the project root. -->
- TODO: e.g. "API service. Express + Postgres. Different lint config than the `web/` package."

## Commands (only those that differ from project root)

- TODO: e.g. `pnpm --filter @org/api test`
- TODO: e.g. `pnpm --filter @org/api db:migrate`

## Local conventions (only what differs)

- TODO: e.g. "Use `pino` for logging, not the project default logger."
- TODO: e.g. "All controllers extend `BaseController` from `src/controllers/_base.ts`."

## Local constraints

- TODO: e.g. "Never import from `packages/web/`."

<!--
DELETE-BEFORE-COMMIT:
- [ ] Nothing here is already stated at project or global level.
- [ ] No README content.
- [ ] ≤ 50 lines.
-->
